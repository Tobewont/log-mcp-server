apiVersion: v1
kind: Service
metadata:
  name: log-mcp-server
  namespace: log-mcp
  labels:
    app.kubernetes.io/name: log-mcp-server
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: log-mcp-server
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
